// Work in progress.

deviceMode = 23; // 16 + Reaper Midi Outputs device id number.
msg_type = 11; // 11 is for CCs.
channel = 0; // actual midi channel - 1.

test = #;

function main() (
  
  defer("main()");
);
main();



StuffMIDIMessage(deviceMode, msg_type * 16 + channel, 0, 127); 
atexit("ShowConsoleMsg(\"Exited\");");



